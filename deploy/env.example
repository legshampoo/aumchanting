# Local dev — copy values into:
#   services/api/.env
#   apps/web/.env.local  (see apps/web/.env.local.example)
#
# Production — same names as GitHub repository secrets (Settings → Secrets → Actions):
#   LIVEKIT_URL
#   LIVEKIT_API_KEY
#   LIVEKIT_API_SECRET
#
PORT=8787
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
