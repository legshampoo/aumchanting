# Local dev: copy repo root .env.example → .env
#
# Production — same names as GitHub repository secrets (Settings → Secrets → Actions):
#   LIVEKIT_URL
#   LIVEKIT_API_KEY
#   LIVEKIT_API_SECRET
#
# Written to /opt/aumchanting/.env on deploy by GitHub Actions.

PORT=8787
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
